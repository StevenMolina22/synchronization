[package]
name = "synchronization"
version = "0.1.0"
edition = "2024"

[dependencies]
env_logger = "0.11"
log = "0.4"
rand = { version = "0.8", features = ["small_rng"] }
tokio = { version = "1", features = ["full"] }
